#include <stdio.h>
#include <stdlib.h>

#ifndef FUNCIONES_H_INCLUDED
#define FUNCIONES_H_INCLUDED

#endif // FUNCIONES_H_INCLUDED

float sumar (float nro1, float nro2);
float restar (float nro1, float nro2);
float dividir (float mro1, float nro2);
float multiplicar (float mro1, float nro2);
float factorial (float nro1);

/** sumar
 * \brief suma los valores ingresados
 * \param primer numero a sumar
 * \param segundo numero a sumar
 * \return devuelve la respuesta de la suma de los valores ingresados
 */

/** restar
 * \brief resta los valores ingresados
 * \param primer numero a restar
 * \param segundo numero a restar
 * \return devuelve la respuesta de la resta de los valores ingresados
 */

/** dividir
 * \brief divide los valores ingresados
 * \param primer numero a dividir
 * \param segundo numero a dividir
 * \return devuelve la respuesta de la division de los valores ingresados
 */

/** multiplicar
 * \brief multiplica los valores ingresados
 * \param primer numero a multiplicar
 * \param segundo numero a multiplicar
 * \return devuelve la respuesta de la multiplicacion de los valores ingresados
 */

 /** factorial
  * \brief saca el factorial de A=X
  * \param numero a factorear
  * \return devuelve el numero factorizado (resultado)
  */



